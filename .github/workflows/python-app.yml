name: testing

on:
  - push
  - pull_request

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
      uses: actions/checkout@v2
    - name: build app
      run: make build_dev
    # - name: run db
    #   run: make run_db
    - name: run tests
      run: make test
